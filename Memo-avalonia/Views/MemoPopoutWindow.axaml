<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:models="using:Memo.Models"
        xmlns:components="using:Memo.Components"
        mc:Ignorable="d"
        d:DesignWidth="360" d:DesignHeight="280"
        x:DataType="models:MemoItem"
        x:Class="Memo.Views.MemoPopoutWindow"
        Title="备忘录"
        Icon="/Assets/appicon.ico"
        Width="360" Height="280"
        MinWidth="280" MinHeight="180"
        ShowInTaskbar="False"
        SystemDecorations="None"
        Background="Transparent"
        TransparencyLevelHint="Transparent">

  <Window.Resources>
    <StreamGeometry x:Key="PopoutToolbarIcon">M4,5H20V7H4ZM4,11H20V13H4ZM4,17H20V19H4Z</StreamGeometry>
  </Window.Resources>

  <Window.Styles>
    <Style Selector="Border.PopoutNoteSurface">
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="BorderBrush" Value="Transparent"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="CornerRadius" Value="12"/>
      <Setter Property="Padding" Value="10.5"/>
      <Setter Property="Transitions">
        <Transitions>
          <BrushTransition Property="BorderBrush" Duration="{DynamicResource MotionStandardDuration}" Easing="CubicEaseOut"/>
          <BrushTransition Property="Background" Duration="{DynamicResource MotionStandardDuration}" Easing="CubicEaseOut"/>
        </Transitions>
      </Setter>
    </Style>
    <Style Selector="Border.PopoutNoteSurface.editing">
      <Setter Property="Background" Value="Transparent"/>
    </Style>
  </Window.Styles>

  <!--
    窗体边缘/四角拖拽缩放手柄样式（与 MainWindow 同构）：
    外壳 Border 内部叠放内容层 + 8 个透明手柄（4 边 + 4 角）。
    边厚 6px；四角 14×14 对齐外壳 CornerRadius=14 的圆角弧线。
    一个共享 handler 通过每个手柄的 Tag 区分要缩放的哪条边/哪个角。
  -->
  <Window.Styles>
    <Style Selector="Border.ResizeEdge">
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="Panel.ZIndex" Value="10"/>
      <Setter Property="BorderBrush" Value="Transparent"/>
      <Setter Property="BorderThickness" Value="1"/>
    </Style>
    <Style Selector="Border.ResizeCorner">
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="BorderBrush" Value="Transparent"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="CornerRadius" Value="0"/>
      <Setter Property="Panel.ZIndex" Value="20"/>
    </Style>
  </Window.Styles>

  <Border x:Name="_popoutShell"
          Background="{StaticResource BgPrimaryBrush}"
          BorderBrush="{StaticResource BorderSubtleBrush}"
          BorderThickness="1"
          CornerRadius="14"
          RenderTransformOrigin="0.5,0.5">
    <Border.RenderTransform>
      <ScaleTransform ScaleX="1" ScaleY="1"/>
    </Border.RenderTransform>

    <Grid RowDefinitions="38,*">
      <Border Grid.Row="0"
              x:Name="_titleBarDrag"
              Background="{StaticResource BgPrimaryBrush}"
              BorderBrush="{StaticResource BorderSubtleBrush}"
              BorderThickness="0,0,0,1"
              CornerRadius="14,14,0,0"
              PointerPressed="OnTitleBarPointerPressed">
        <Grid ColumnDefinitions="Auto,*,Auto" Margin="12,0,8,0">
          <StackPanel Grid.Column="0"
                      Orientation="Horizontal"
                      VerticalAlignment="Center"
                      Spacing="8">
            <Image Source="/Assets/appicon.png" Width="18" Height="18"/>
            <TextBlock x:Name="_titleText"
                       Text="{Binding Title}"
                       VerticalAlignment="Center"
                       FontWeight="SemiBold"
                       FontSize="13"
                       Foreground="{StaticResource TextPrimaryBrush}"
                       TextTrimming="CharacterEllipsis"
                       MaxWidth="190"/>
          </StackPanel>

          <Border Grid.Column="1"
                  Background="Transparent"/>

          <StackPanel Grid.Column="2"
                      Orientation="Horizontal"
                      VerticalAlignment="Center"
                      Spacing="2">
            <Button Classes="TitleBarBtn" CornerRadius="6" Click="OnToolbarToggle" x:Name="_toolbarButton" ToolTip.Tip="显示 Markdown 工具栏" Width="26" Height="26" MinWidth="26" MinHeight="26">
              <PathIcon Data="{StaticResource PopoutToolbarIcon}" Width="13" Height="13" Foreground="{StaticResource IconDefaultBrush}"/>
            </Button>
            <Button Classes="TitleBarBtn" CornerRadius="6" Click="OnPinToggle" x:Name="_pinButton" ToolTip.Tip="置顶" Width="26" Height="26" MinWidth="26" MinHeight="26">
              <PathIcon Data="{StaticResource PinIcon}" Width="13" Height="13" Foreground="{StaticResource IconDefaultBrush}">
                <PathIcon.RenderTransform>
                  <RotateTransform Angle="0"/>
                </PathIcon.RenderTransform>
              </PathIcon>
            </Button>
            <Button Classes="TitleBarBtn CloseBtn" CornerRadius="6" Click="OnCloseClick" ToolTip.Tip="关闭" Width="26" Height="26" MinWidth="26" MinHeight="26">
              <PathIcon Data="{StaticResource CloseIcon}" Width="13" Height="13" Foreground="{StaticResource IconDefaultBrush}"/>
            </Button>
          </StackPanel>
        </Grid>
      </Border>

      <Border Grid.Row="1"
              Background="{StaticResource BgPrimaryBrush}"
              CornerRadius="0,0,14,14">
        <Grid RowDefinitions="Auto,*" Margin="8,1,8,8">
          <Grid Grid.Row="0"
                Cursor="Hand"
                Tapped="OnTimeTapped"
                Margin="0,5,0,0">
            <TextBlock x:Name="_timeFullText"
                       Text="{Binding FullTime}"
                       HorizontalAlignment="Center"
                       FontSize="11"
                       FontWeight="Medium"
                       Foreground="{StaticResource TextTertiaryBrush}"
                       LetterSpacing="0.3"
                       Margin="0,0,0,1"
                       Opacity="0"/>
            <TextBlock x:Name="_timeShortText"
                       Text="{Binding RelativeTime}"
                       HorizontalAlignment="Center"
                       FontSize="11"
                       FontWeight="Medium"
                       Foreground="{StaticResource TextTertiaryBrush}"
                       LetterSpacing="0.3"
                       Margin="0,0,0,1"
                       Opacity="1"/>
          </Grid>

          <Border Grid.Row="1"
                  x:Name="_noteSurface"
                  Classes="PopoutNoteSurface"
                  Margin="0,-5,0,0">
            <components:MarkdownEditor x:Name="_markdownEditor"
                                       HideToolbarInPreview="True"
                                       BlendBordersInPreview="True"
                                       UseBorderlessChrome="True"
                                       HorizontalAlignment="Stretch"
                                       VerticalAlignment="Stretch"/>
          </Border>
        </Grid>
      </Border>

      <!-- 边缘/四角缩放手柄（覆盖在内容之上，RowSpan 横跨 38,* 两行） -->
      <Border Classes="ResizeEdge" Grid.Row="0" Grid.RowSpan="2"
              Tag="Top"
              Height="6" VerticalAlignment="Top" Margin="0"
              PointerPressed="OnResizeHandlePointerPressed"/>
      <Border Classes="ResizeEdge" Grid.Row="0" Grid.RowSpan="2"
              Tag="Bottom"
              Height="6" VerticalAlignment="Bottom" Margin="0"
              PointerPressed="OnResizeHandlePointerPressed"/>
      <Border Classes="ResizeEdge" Grid.Row="0" Grid.RowSpan="2"
              Tag="Left"
              Width="6" HorizontalAlignment="Left" Margin="0"
              PointerPressed="OnResizeHandlePointerPressed"/>
      <Border Classes="ResizeEdge" Grid.Row="0" Grid.RowSpan="2"
              Tag="Right"
              Width="6" HorizontalAlignment="Right" Margin="0"
              PointerPressed="OnResizeHandlePointerPressed"/>
      <Border Classes="ResizeCorner" Grid.Row="0" Grid.RowSpan="2"
              Tag="TopLeft"
              Width="14" Height="14" HorizontalAlignment="Left" VerticalAlignment="Top"
              PointerPressed="OnResizeHandlePointerPressed"/>
      <Border Classes="ResizeCorner" Grid.Row="0" Grid.RowSpan="2"
              Tag="TopRight"
              Width="14" Height="14" HorizontalAlignment="Right" VerticalAlignment="Top"
              PointerPressed="OnResizeHandlePointerPressed"/>
      <Border Classes="ResizeCorner" Grid.Row="0" Grid.RowSpan="2"
              Tag="BottomLeft"
              Width="14" Height="14" HorizontalAlignment="Left" VerticalAlignment="Bottom"
              PointerPressed="OnResizeHandlePointerPressed"/>
      <Border Classes="ResizeCorner" Grid.Row="0" Grid.RowSpan="2"
              Tag="BottomRight"
              Width="14" Height="14" HorizontalAlignment="Right" VerticalAlignment="Bottom"
              PointerPressed="OnResizeHandlePointerPressed"/>
    </Grid>
  </Border>
</Window>
